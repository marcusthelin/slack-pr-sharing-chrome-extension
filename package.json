{
  "name": "slack-pr-sharing",
  "version": "1.0.0",
  "description": "Chrome extension to share GitHub PRs to Slack",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && npx @tailwindcss/cli -i ./src/input.css -o ./dist/styles.css && tsc && pnpm run copy-files",
    "copy-files": "cp src/manifest.json dist/ && cp src/popup.html dist/",
    "watch": "tsc --watch",
    "watch:css": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/styles.css --watch"
  },
  "keywords": [
    "chrome-extension",
    "github",
    "slack",
    "pull-request"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@tailwindcss/cli": "4.1.11",
    "@types/chrome": "^0.0.329",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "4.1.11",
    "typescript": "^5.8.3"
  }
}
